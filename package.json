{
  "name": "aura-space-sadec",
  "version": "2.1.0",
  "description": "AURA SPACE — Quán cà phê container industrial-luxury tại Sa Đéc",
  "type": "module",
  "scripts": {
    "lint": "eslint js/ worker/src/ --ext .js",
    "lint:fix": "eslint js/ worker/src/ --ext .js --fix",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "test:ci": "jest --ci --coverage --maxWorkers=2",
    "dev": "vite",
    "build": "npm run lint && vite build",
    "preview": "vite preview",
    "deploy": "bash deploy-cloudflare.sh",
    "clean": "rm -rf dist/"
  },
  "keywords": [
    "fnb",
    "cafe",
    "container",
    "sa-dec",
    "restaurant"
  ],
  "author": "Mekong CLI",
  "license": "MIT",
  "devDependencies": {
    "@babel/core": "^7.29.0",
    "@babel/preset-env": "^7.29.0",
    "babel-jest": "^30.3.0",
    "eslint": "^9.0.0",
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0",
    "terser": "^5.27.0",
    "vite": "^8.0.3"
  }
}
