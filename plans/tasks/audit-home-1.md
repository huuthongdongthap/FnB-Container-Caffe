# Phase 7: Home Page UI & Structure Audit

ĐỌC: `index.html` toàn bộ.

## Nhiệm vụ (Audit):
1. **Kiểm tra CSS/JS Inline:** Có quá nhiều CSS (`<style>`) và JS nội tuyến chưa? Đề xuất bước để tách ra `css/index.css` và khối JS xuống cuối trang.
2. **Unified Navigation:** Trang `index.html` đã có `<nav>` và `<footer>` dùng chung (từ unified-navigation Phase 1) chưa? Nếu chưa, cần patch vào chỗ nào?
3. **Responsive Check:** Phân tích các khối `@media (max-width: 768px)` xem có đủ coverage cho Mobile & Tablet chưa.

Trả về danh sách BULLET POINT các lỗi tìm thấy và đề xuất sửa chữa ngắn gọn. KHÔNG code ngay. Kết thúc câu trả lời bằng "Done audit-7.1".
